<h1>Ingresar</h1>
<div class="row">
<div class="container">

<%= form_for(:session, url: sessions_create_path) do |f|%> <!--Este modelo usa la ruta a la que va a ir, no variables. Eso es lo que hay que especificarle. Ojo con eso-->

    <div class="form-group">
        <%= f.label 'e-mail'%>
        <%= f.text_field :email, class:"form-control"%>
    </div>

    <div class="form-group">
        <%= f.label 'Clave'%>
        <%= f.password_field :password, class:"form-control"%>
    </div>


    <%= f.submit "Entrar", class:"btn btn-primary"%>

<%end%>
